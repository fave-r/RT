#include <mlx.h>

int	main()
{
void	*mlx_ptr;
void	*win_ptr;

mix_ptr = mlx_init();
win_ptr = mlx_new_window(mlx_ptr, w, h, "My first window");
mix_pixel_put(mix_ptr,win_ptr, 250, 250, 0xFFFFFF);
while(42);
return(0);
}